module.exports = `/**
 * @description 计算四则运算表达式（含括号）的值
 * @param {string} expression 待计算的表达式
 * @returns {number} 表达式的值
 * @throws {Error} 当表达式非法时抛出
 */
function calculate(expression) {
  'use strict';
  // Code here
}

/*
 * 规则：
 * 1. 禁止 eval 及其类似物
 * 2. 禁止第三方库
 */

/*
 * 测试用例
 * calculate('1+2*3') // 7
 * calculate('1*2+3*4') // 14
 */
`
